apiVersion: v1
kind: Service
metadata:
  name: {{ include "claimflow.fullname" . }}
  labels:
    {{- include "claimflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "claimflow.selectorLabels" . | nindent 4 }}
